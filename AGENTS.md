# type-pm-mech2 作業規則

このリポジトリは，Type-PM の新しい独立した型付け体系を一から機械化する．

- `type-pm-mech` の実装，互換層，旧仕様専用の回帰をこのリポジトリへ持ち込まない．新しい仕様に必要な定義だけを独立に実装する．
- 旧実装との比較が必要な場合は sibling repository の `type-pm-mech` を別に実行し，このリポジトリ内に旧推論器を再現しない．
- 未着手の言語機能を先回りして構文や定理へ追加しない．各 milestone は独立して build できる状態にする．
- 実行可能な推論器や旧 terminal audit を，宣言的型付けの定義へ混ぜない．
- 新しい公開定理には `sorry`，`admit`，追加公理を使わない．
- 検証はリポジトリ直下で `lake build` を実行する．
- まとまりのある変更が完了し，`lake build`と必要な個別回帰が通った時点で，個別の指示を待たずに適宜commitとpushを行う．大きな作業では，独立して検証できる意味のある区切りごとに行ってよい．
- 未完成または未検証の状態を保存するだけのcommitは作らない．ユーザーの無関係な変更と安全に分離できない場合，検証が失敗している場合，またはpush先が不明な場合はcommitとpushを行わず，状況を報告する．
- ユーザーがその作業についてcommitまたはpushを行わないよう明示した場合は，その指示を優先する．commit messageに`Co-Authored-By`を付けない．
