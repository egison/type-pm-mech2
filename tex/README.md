# type-pm-mech2 入門解説

`type-pm-mech2-guide.tex`は，現行の`type-pm-mech2`実装を初めて読む人のための
日本語解説である．旧`type-pm-mech`の設計ではなく，このリポジトリのLeanコードと
公開定理を基準にしている．

PDFはこのディレクトリで次のように生成する．

```sh
make
```

生成物は`type-pm-mech2-guide.pdf`である．補助ファイルだけを削除する場合は
`make clean`，PDFも含めて削除する場合は`make cleanAll`を使う．
